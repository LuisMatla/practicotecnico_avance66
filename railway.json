{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd RASA && pip install -r requirements.txt && rasa train"
  },
  "deploy": {
    "startCommand": "cd RASA && python rasa_railway_entry.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

